# Normalize all text files to LF in the repo; editors handle display
* text=auto eol=lf

# Windows-specific files that must stay CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files — no line-ending conversion
*.png binary
*.jpg binary
*.ico binary
*.pfx binary
