{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://mysticforge:devpassword@localhost:5432/mysticforge"
      ]
    }
  }
}
